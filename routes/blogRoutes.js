import e from "express";

const router = e.Router();

//Get all blogs
router.get('/', async(request, response) => {
    try{
        
    }catch(err){
        console.log(err);
        
    }
})

// Get one blog


export default router;